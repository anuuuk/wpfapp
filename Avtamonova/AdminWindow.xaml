<Window x:Class="Avtamonova.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Avtamonova"
        mc:Ignorable="d"
        Title="Администратор" SizeToContent="WidthAndHeight">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">

            <Button Content="Управление сотрудниками" Margin="10" MinHeight="50" MinWidth="200" Background="#F5F5DC" Click="Button_Click"/>
            <Button Content="Управление заказами" Margin="10" MinHeight="50" MinWidth="200" Background="#F5F5DC"/>
            <Button Content="Управление сменами" Margin="10" MinHeight="50" MinWidth="200" Background="#F5F5DC"/>
            <Button Content="Выйти" Margin="10" MinHeight="50" MinWidth="200" Background="#D9534F"/>
            
        </StackPanel>

    </Grid>
</Window>
